@inherits LayoutComponentBase

<RadzenDialog />
 <MatPortalHost></MatPortalHost>

<MatDrawerContainer Style="width: 100vw; height: 100vh;">
    <MatDrawer @bind-Opened="@Opened" Mode="@MatDrawerMode.Modal" @ref="MenuLateral">
        <NavMenu />
    </MatDrawer>
    <MatDrawerContent>
        <MenuBar MatDrawer="MenuLateral" />
        @Body
    </MatDrawerContent>
</MatDrawerContainer>


@code{

    private MatDrawer MenuLateral { get; set; }

    bool Opened = false;

}
